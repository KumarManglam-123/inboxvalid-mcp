{
  "name": "inboxvalid-mcp",
  "version": "1.0.0",
  "description": "Email verification MCP server",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc && node -e \"import('node:fs').then(fs => fs.copyFileSync('src/disposableDomains.json', 'dist/disposableDomains.json'))\"",
    "start": "node dist/server.js",
    "test": "vitest run",
    "demo": "tsx test/demo.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3",
    "vitest": "^3.0.6"
  }
}
